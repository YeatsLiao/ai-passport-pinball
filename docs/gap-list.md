# 差距清单：原版规格 vs 当前实现

对照基准：[`space-cadet-spec.md`](./space-cadet-spec.md)（下称"规格"，条目号引用其章节）。
被对照代码：`main/pb_game.c`、`main/pb_game.h`、`main/pb_table.c/.h`、`main/pb_render.c`、
`tools/pb_art.py`（提交 `153b81d`）。

每条格式：**规格要求 → 当前实现 → 处置**。处置为 `FIX` 的条目在本轮实现，
`KEEP` 为已一致，`BOARD` 为需上板确认，`OUT` 为规格 §6 已声明不移植。

> 本文是第二步的历史快照。逐条“一致 / 已修复 / 待上板确认”的当前状态见
> [`spec-compliance.md`](./spec-compliance.md)；实现过程中回源码复核发现的
> 本文 B2/B8 两处误判已在原文内直接修正。

## A. 致命玩法缺陷（用户报"无限球 / 球数不涨 / 逻辑没连上"）

| # | 规格 | 当前实现 | 处置 |
| --- | --- | --- | --- |
| A1 | §1 拓扑：黑洞在**两挡板之间的落球口**，只有没接住的球才进 | `PB_HOLE_Y 248` 位于弹弓之间、挡板上方 32px 的必经落球线上，捕获半径 7 → 几乎每颗下落球被吞并从顶部车道弹出，球数长期不推进 | **FIX** 移到 (107,300) 落球口，半径 8 |
| A2 | §2.4 `a_kout3` 黑洞 = 20000 + 踢出后冷却（`TKickout` L67-L74） | `SCORE_HOLE 2500 × (wh_chain+1)`，冷却 4.0s | **FIX** 20000，冷却 6.0s |
| A3 | §4.3 球保存 5.0s，发射瞬间武装一次 | `PB_BALL_SAVE_S 8.0f` | **FIX** 5.0f |
| A4 | §4.6 分数无 7 位截断（1e9 进位） | `score > 9999999` 直接封顶 → 高分被截断，榜单/持久化都失真 | **FIX** 上限 99999999（面板 8 位宽），并注明显示位宽限制 |
| A5 | §5 5 槽最高分表 + 插入下移 + 校验和 | 单槽 `high_score` u32 | **FIX** 5 槽表 + 校验和 blob |
| A6 | §4.3/§4.4 掉球时球数按规则推进；无兜底玩法 | 2.2s 丢球保险兜底 `on_drain` | **KEEP**（健壮性，不计分、不改玩法；规格 §4.7 之外的异常恢复） |
| A7 | §1 分区 J 弹出方向：踢出洞把球弹回挡板区 | 黑洞弹到随机车道口 (lane_x,56)，可能卡在车道分隔柱 (87/127, 25-54) | **FIX** 改为向上踢回挡板区，避开分隔柱 |

## B. 得分点分值与触发来源（规格 §2/§3）

| # | 规格 | 当前实现 | 处置 |
| --- | --- | --- | --- |
| B1 | §2.1 pop bumper `control_bump_scores1[BmpIndex]` = 500/1000/1500/2000，BmpIndex 为**持久升级档位**（§3 由升级灯组满组 +1，RC 5 "Weapons Upgraded"） | `SCORE_BUMP × tier`，tier 取 `bump_combo`（本球内连续命中数），掉球归零 | **FIX** `bump_tier` 0..3 持久档位，由 §3 升级链推进 |
| B2 | §3 bumper 升级链（回源码修正后的准确描述）：挡板挥动 → `bmpr_inc_lights.Message(24/25)` 循环移位；**再入车道某盏由灭变亮时**若该组已亮数 == 总数 → 清组 + `attack_bump.Message(12)` = BmpIndex+1（RC 5） | 顶部车道全亮 → 直接加**倍率**，与 bumper 无关 | **FIX** 车道切换灯检查 `bump_prog` 满组 → 清组 + `bump_tier` +1（推进方式见偏差 D2） |
| B3 | §3 再入车道灯为**切换式**（亮→灭 / 灭→亮），仅灭→亮时推进灯组 | `on_lane` 只点亮不清灭，全亮后立即强制清组 | **FIX** 改切换语义 |
| B4 | §4.2 `score_multipliers[5] = {1,2,3,5,10}`，索引 0..4，每球结束归 0 | `mult` 即乘数，范围 1..5（`PB_MULT_MAX 5`） | **FIX** `mult_idx` 0..4 + 值表 {1,2,3,5,10} |
| B5 | §2.3 倍率来源 = 倍率目标组（单个 500、三个全倒 1500 + 升档，RC 56-59） | 左侧三掉落目标：单个 1500、组清空 50000，提示文字写 "BONUS 5000"（**文字与实际分值不一致**） | **FIX** 单个 500、组完成 1500 + `mult_idx` +1，提示改 "MULTIPLIER xN" |
| B6 | §2.1 `rebo3/4` 回弹器 500；pop bumper 才有档位分 | 8 个 circle（3 个 kick>0 的 bumper + 5 个 kick==0 的小立柱）**全部**按 `SCORE_BUMP × tier` 计分，小立柱最高白拿 2000 | **FIX** 按 `circles[i].kick > 0` 分流：bumper 走档位分，立柱走 500 |
| B7 | §2.4 `a_kout1` 引力井 50000 + 冷却（RC 82） | 左上洞被当作"虫洞"，2500×chain，文案 "WORMHOLE!" | **FIX** 改引力井：50000、冷却 8.0s、文案 "GRAVITY WELL" |
| B8 | §2.4 `a_kout2` hyperspace 按 `hyper_lights` 点数 0..4 取 `control_kickout_score1[0/2/3/4]` = 10000/20000/50000/150000，第 4 档清环（`HyperspaceKickOutControl` L2208-L2259） | `HS_SCORES[4]` 取值本身正确，150000 档第 4 次命中**可以**拿到；偏差在于：① 满 4 后不清环而是永久停在 150000；② 球落 y>265 就重置连击（自创规则） | **FIX** `hs_lights = (hs_lights+1) % 4` 满 4 清环；删除 y>265 重置 |
| B9 | §2.4 `sink1/2/3` 虫洞 2500/5000/7500 按**目的灯匹配状态**取值，且依赖 multiball 锁球 | `wh_chain` 自创连击计数 | **FIX** 删除 `wh_chain` 与 `SCORE_HOLE`（虫洞 sink 属规格 §6 OUT） |
| B10 | §2.2 再入车道 2000 | `SCORE_LANE 2000` | **KEEP** |
| B11 | §2.1 弹弓回弹器 500 | `SCORE_SLING 500` | **KEEP** |
| B12 | §3 军衔：9 级（`RankRcArray` 84-92），晋升文案 RC 83 "Promotion to %s"，`AddRankProgress` **不计分** | 5 级缩写 CDT/ENS/LT/CPT/ADM；晋升奖 `SCORE_RANK × rank`、封顶后再奖 `SCORE_FUELMAX`（**自创分值**） | **FIX** 9 级缩写表；晋升不计分，删除 `SCORE_RANK`/`SCORE_FUELMAX` |
| B13 | §3 军衔进度来源 = `AddRankProgress` 点亮 `outer_circle`，燃料侧由燃料车道/燃料目标驱动 | 每 3 次 bumper 命中点一盏 + 每次放倒目标点一盏（**两处都是自创**） | **FIX** 仅"倍率目标组完成"推进 1 段（§6 声明的等价移植载体）；删除 bumper 点灯与 `bump_hits` |
| B14 | §2.1-2.4 所有入账经 `AddScore` 乘 `score_multipliers` | `add_score` 已乘 `mult` | **KEEP** |
| B15 | §4.4 额外球 / replay | 无来源（依赖虫洞 sink，属 OUT） | **OUT** |
| B16 | §2.2 outlane 20000 / return lane / bonus lane / 燃料车道 / space warp / ramp / flag / oneway4 / 勋章·任务·危险目标 | 台面元素不存在 | **OUT**（规格 §6 已声明） |

## C. UI 层级与文字错乱（规格 §1 分区 E、§3 文本框）

| # | 规格 | 当前实现 | 处置 |
| --- | --- | --- | --- |
| C1 | §3 `info_text_box` 是独立信息层，不与台面图形争位 | `lbl_msg` 居中于 `CENTER + 28` → 覆盖 (45..195, 188..204)，正压在徽章弧字 "SPACE CADET"、行星与 "MISSION READY" 上 | **FIX** 背景烘焙专用信息带（规格 §6 载体），`lbl_msg` 落入带内不透明显示 |
| C2 | §1 分区 E：中央环形为灯组区，同一位置不得叠两套灯 | 燃料灯 i=2 落在 (107,145)，与装饰菱形灯插 (107,143) 和小立柱 bumper 帽精灵 (107,143.7-156.3) **三重重叠** | **FIX** 删除装饰菱形灯插，该三位置改作 §3 bumper 升级灯组的暗灯座 |
| C3 | §3 得分飘字出现在命中点 | 晋升/燃料飘字固定投在 (107,150)，与 C2 同一冲突点 | **FIX** 晋升不计分 → 无飘字，仅信息带提示（同时满足 B12） |
| C4 | §1 分区 L 记分板独立于台面 | `R.overlay` 全屏 240×320 + `LV_OPA_90`，暂停/标题/结算时**盖住记分板**，看不到分数与球数 | **FIX** overlay 只覆盖台面区（y 从 `PB_STATUS_H` 起） |
| C5 | §1 分区 F 右上区元素应可读 | 引力井/hyperspace 洞画在暗角，视觉上像落在台面外（实际在 `BED_POLY` 内） | **FIX** 洞缘加亮环 + 台面色底，确保可读 |
| C6 | §5 榜单可见 | 标题页只显示单一 HIGH | **FIX** 标题页/结算页显示 5 行榜单 |
| C7 | 面板文字不得互压 | `lbl_fuel`/`lbl_rank` 起点 y=205，面板标题 "FUEL"/"RANK" 基线 y≈202.5，行距不足 | **FIX** 动态值下移到 y=210，与标题分两行 |
| C8 | 暂停菜单与台面文字明显区分 | 金色边框卡片 + 16 号选项 ✓ | **KEEP** |
| C9 | README 玩法段应与实现一致 | 写的是 bumper 1000 / 弹弓 250 / 目标 500 / 车道 100 / 8s 球保存 / 车道→倍率，**全部与代码和规格不符** | **FIX** 按本轮实现重写 |

## D. 需上板确认（无法由编译/预览判定）

| # | 项 | 判据 |
| --- | --- | --- |
| D1 | 引力井 (21,68) 与 hyperspace (196,68) 的实际触发频率 | 通道净宽 ~16px、捕获半径 5；需实测一局内触发次数是否 >0 且不至于过频 |
| D2 | 黑洞移到 (107,300) 后的手感 | 应表现为"没接住的球偶尔被吐回"，不得再出现球数不涨 |
| D3 | 6.0s/8.0s 冷却时长与 5.0s 球保存的实际节奏 | 一局 3 球耗时应明显短于修复前 |
| D4 | 信息带 (规格 §6) 与 bumper 升级灯座在 ST7789 上的可读性 | 实拍确认 |
| D5 | 5 行榜单 + 9 级军衔缩写在 42px 面板内的显示 | 预览图可判，仍需实拍复核 |
